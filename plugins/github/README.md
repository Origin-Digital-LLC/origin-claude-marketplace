# github

Skills for working with any GitHub repository. Requires the [GitHub CLI](https://cli.github.com/) (`gh`) to be installed and authenticated.

## Skills

### fetch-pr-review

Fetches the current PR with all review comments so Claude can resolve them and post responses back to GitHub.

## Requirements

- `gh` CLI installed and authenticated (`gh auth login`)
